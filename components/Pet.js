import {Alert, Modal, StyleSheet, SafeAreaView, Text, Pressable, View, TextInput} from 'react-native';

export default function Pet() {
    return(
        <SafeAreaView>
            <Text>This is a pet eeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeee</Text>
        </SafeAreaView>
    )
}

const styles = StyleSheet.create({
    general: {
        flex: 1,
        justifyContent: 'center',
        backgroundColor: '#FFC0CB',
        padding: 8,
    },

    centeredView: {
      flex: 1,
      justifyContent: 'center',
      alignItems: 'center',
      marginTop: 20,
    },
  });